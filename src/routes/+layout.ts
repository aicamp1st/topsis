// Aplikasi 100% client-side: data tersimpan di localStorage browser.
// SSR dimatikan, tiap route di-prerender sebagai shell statis untuk GitHub Pages.
export const ssr = false
export const prerender = true
