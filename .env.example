# Lokal (development) — file SQLite
DATABASE_URL=file:local.db

# Produksi (Turso) — isi di Vercel Environment Variables
# DATABASE_URL=libsql://<nama-db>-<org>.turso.io
# DATABASE_AUTH_TOKEN=<turso-auth-token>
